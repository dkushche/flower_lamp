#!/usr/bin/env bash

cd "$(dirname "$0")/.."

source .env

docker run --rm                    \
    -u $UID                        \
    -v ./$PROJECT_NAME:/project    \
    -w /project                    \
    espressif/idf:$ESP_IDF_VERSION \
    idf.py build
